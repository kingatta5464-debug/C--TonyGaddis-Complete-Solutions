#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>
#include <ctime>
// #include <cstdlib>
#include <conio.h>
using namespace std;

int main()
{
    // double max_credit;
    // double used_credit, availible_credit;
    // cout<<"Enter Maximum Credit : ";
    // cin>>max_credit;
    // cout<<"Amount of Credit Used : ";
    // cin>>used_credit;
    // availible_credit=max_credit-used_credit;
    // cout<<"Availible Credit : "<<setw(4)<<availible_credit;

    // int number1, number2;
    // float quotient;
    // cout << "Enter two numbers and I will divide\n";
    // cout << "the first by the second for you.\n";
    // cin >> number1>> number2;
    // quotient = float  (number1) / number2;
    // cout << quotient;

    // // Assigning random values to const variables
    // const int number1 = 49;
    // const int number2 = 7;

    // // Perform some operations with these values
    // const int sum = number1 + number2;
    // const int difference = number1 - number2;
    // const int product = number1 * number2;
    // const double quotient = static_cast<double>(number1) / number2;

    // // Output the results
    // std::cout << "Number 1: " << number1 << std::endl;
    // std::cout << "Number 2: " << number2 << std::endl;
    // std::cout << "Sum: " << sum << std::endl;
    // std::cout << "Difference: " << difference << std::endl;
    // std::cout << "Product: " << product << std::endl;
    // std::cout << "Quotient: " << quotient << std::endl;

    // int number1, number2;
    // cout << "Enter two numbers and I will multiply\n";
    // cout << "them by 50 for you.\n";
    // cin >> number1 >> number2;
    // number1 *= 50;
    // number2 *= 50;
    // cout << number1 << " " << number2;

    // double number, half;
    // cout << "Enter a number and I will divide it\n";
    // cout << "in half for you.\n";
    // cin >> number;
    // cout<<number;
    // half = number/ 2;
    // cout<< fixed << showpoint  << half << endl;

    // double salary, monthly;
    // cout << "What is your annual salary? ";
    // cin >> salary;
    // monthly = (salary) / 12;
    // cout << "Your monthly wages are " << monthly << endl;

    // long x, y, z;
    // x = y = z = 4;
    // x += 2;
    // y -= 1;
    // z *= 3;
    // cout << x << " " << y << " " << z << endl;

    // long seconds;
    // double minutes, hours, days, months, years;
    // cout << "Enter the number of seconds that have\n";
    // cout << "elapsed since some time in the past and\n";
    // cout << "I will tell you how many minutes, hours,\n";
    // cout << "days, months, and years have passed: ";
    // cin >> seconds;

    // minutes = seconds / 60;
    // hours = minutes / 60;
    // days = hours / 24;
    // years = days / 365;
    // months = years * 12;
    // cout << setprecision(4) << fixed << showpoint << right;
    // cout << "Minutes: " << setw(6) << minutes << endl;
    // cout << "Hours: " << setw(6) << hours << endl;
    // cout << "Days: " << setw(6) << days << endl;
    // cout << "Months: " << setw(6) << months << endl;
    // cout << "Years: " << setw(6) << years << endl;

    // float capacity;
    // cout<<"How many gallons car can hold? ";
    // cin>>capacity;
    // float miles;
    // cout<<"How many miles it cover when tank is full? ";
    // cin>>miles;
    // float pg;
    // pg=miles/capacity;
    // cout<<setprecision(6)<<fixed<<showpoint
    //     <<"Per gallon miles are: "<<pg<<endl;

    // float a, b, c;
    // cout << "Total tickets sold of class A: ";
    // cin >> a;
    // cout << "Total tickets sold of class B: ";
    // cin >> b;
    // cout << "Total tickets sold of class C: ";
    // cin >> c;

    // cout << setprecision(2) << fixed << showpoint;

    // cout << "Total earning from class A: ";
    // cout << a * 15 << "$" << endl;
    // cout << "Total earning from class B: ";
    // cout << b * 12 << "$" << endl;
    // cout << "Total earning from class C: ";
    // cout << c * 9 << "$" << endl;

    // cout << "Total earning from all classes: " << (a * 15) + (b * 12) + (c * 9);

    // int s1,s2,s3,s4,s5;
    // cout<<"Enter five test scores : ";
    // cin>>s1>>s2>>s3>>s4>>s5;
    // cout<<setprecision(1)<<fixed<<showpoint;
    // cout<<"Average of test scores are : "<<float(s1+s2+s3+s4+s5)/5<<endl;

    // string m1,m2,m3;
    // float r1,r2,r3;
    // cout<<"Enter 1st Month Name : ";
    // cin>>m1;
    // cout<<"Amount of Rain (in inches) in "<<m1<<" :";
    // cin>>r1;

    // cout<<"Enter 2nd Month Name : ";
    // cin>>m2;
    // cout<<"Amount of Rain (in inches) in "<<m2<<" :";
    // cin>>r2;

    // cout<<"Enter 3rd Month Name : ";
    // cin>>m3;
    // cout<<"Amount of Rain (in inches) in "<<m3<<" :";
    // cin>>r3;

    // cout<<"The average rainfall for "<<m1<<" ,"<<m2<<" and "<<m3<<" is "<<(r1+r2+r3)/3<<" inches.";

    // int male,female;
    // cout<<"number of male : ";
    // cin>>male;
    // cout<<"number of female : ";
    // cin>>female;
    // int total=male+female;
    // cout<<"percentage of male : "<<((float)male/total)*100<<"%"<<endl;
    // cout<<"percentage of female : "<<((float)female/total)*100<<"%"<<endl;

    // float sugar_cup,butter_cup,flour_cup;
    // sugar_cup=1.5/48.0;
    // butter_cup=1.0/48;
    // flour_cup=2.75/48;
    // int no_of_cookies;

    // cout<<"Enter the number of cookies you wanted to make : ";
    // cin>>no_of_cookies;

    // cout<<"Amount of the ingredients needed : "<<endl;
    // cout<<"Sugar : "<<sugar_cup*no_of_cookies<<" cups"<<endl;
    // cout<<"Butter : "<<butter_cup*no_of_cookies<<" cups"<<endl;
    // cout<<"Flour : "<<flour_cup*no_of_cookies<<" cups"<<endl;

    // string name;
    // int ta,tc;
    // cout<<"Enter Movie Name : ";
    // cin>>name;
    // cout<<"Adult tickets sold : ";
    // cin>>ta;
    // cout<<"Child tickets sold : ";
    // cin>>tc;
    // float bo=(ta*10.0)+(tc*6.0);
    // cout<<"Gross Box Office Profit : "<<bo<<" $"<<endl;
    // float tp= ((ta*10.0)+(tc*6.0))*(20.0/100.0);
    // cout<<"Theater payment : "<<tp<<" $"<<endl;
    // float distributor=bo-tp;

    // cout<<"Amount Paid to Distributor : "<<distributor<<" $"<<endl;

    // int cookies;
    // cout<<"How many cookies you ate? ";
    // cin>>cookies;
    // float calories=3000.0/30.0;
    // cout<<"Number of calories you consumed are : "<<(cookies*calories)<<" calories"<<endl;

    // float replacement_cost;
    // cout<<"Enter replacement cost : ";
    // cin>>replacement_cost;
    // cout<<setprecision(4)<<showpoint<<fixed;
    // cout<<"Minimum amount of insurance you should buy : "<<replacement_cost*(80.0/100.0);

    // float tc;
    // cout<<"Enter celsius temperature : ";
    // cin>>tc;
    // cout<<"Fahrenheit teperature is : ";
    // float tf;
    // tf=((9.0/5.0)*tc)+32;
    // cout<<showpoint<<fixed<<setprecision(2)<<tf;

    // double dollar , yen,euros;
    // double yen_per_dollar=98.93;
    // double euros_per_dollar=0.74;

    // cout<<"enter dollars : ";
    // cin>>dollar;
    // cout<<"yen in "<<dollar<<"$ are : "<<dollar*yen_per_dollar<<endl;
    // cout<<"euros in "<<dollar<<"$ are : "<<dollar*euros_per_dollar<<endl;

    // cout<<"enter yen : ";
    // cin>>yen;
    // cout<<"dollars in "<<yen<<"$ are : "<<yen/yen_per_dollar<<endl;

    // cout<<"enter euros : ";
    // cin>>euros;
    // cout<<"dollars in "<<euros<<"$ are : "<<euros/euros_per_dollar<<endl;

    // string month;
    // int year;
    // double total_amount;
    // cout<<"Month : ";
    // cin>>month;
    // cout<<"Year : ";
    // cin>>year;
    // cout<<setprecision(2)<<showpoint<<fixed;
    // cout<<"Total amount collected : ";
    // cin>>total_amount;
    // int country_tax,state_tax;

    // double product_sales;
    // product_sales=total_amount/1.06;
    // cout<<"Sales : "<<product_sales;

    // cout<<"Country Sales Tax : "<<product_sales*(2.0/100.0)<<endl;
    // cout<<"State Sales Tax : "<<product_sales*(4.0/100.0)<<endl;
    // cout<<"Total Sales Tax : "<<(product_sales*(4.0/100.0))+(product_sales*(2.0/100.0))<<endl;

    //  // Constants for assessment percentage and tax rate
    // const double ASSESSMENT_RATE = 0.60;
    // const double TAX_RATE_PER_100 = 0.75;

    // // Input: Actual value of the property
    // double actualValue;
    // cout << "Enter the actual value of the property: $";
    // cin >> actualValue;

    // // Calculate assessment value
    // double assessmentValue = actualValue * ASSESSMENT_RATE;

    // // Calculate property tax
    // double propertyTax = (assessmentValue / 100) * TAX_RATE_PER_100;

    // // Output: Display the assessment value and property tax
    // cout << fixed << setprecision(2); // Set the format to fixed-point notation with 2 decimal places
    // cout << "Assessment value: $" << assessmentValue << endl;
    // cout << "Property tax: $" << propertyTax << endl;

    // double actual_value,assessed_value;
    // cout<<"Enter actual value : ";
    // cin>>actual_value;
    // assessed_value=actual_value*(60.0/100);
    // cout<<"Assessed value will be : "<<assessed_value<<endl;
    // double tax, senior_citizen_exemption=5000.0,remaining_value;
    // remaining_value=assessed_value-senior_citizen_exemption;
    // cout<<"Enter the tax rate per 100$ assessed value : ";
    // cin>>tax;
    // double payable_tax=(remaining_value/100.0)*tax;
    // cout<<"Annual property tax senior citizen have to pay is : "<<payable_tax;
    // double kist;
    // kist=payable_tax/4.0;
    // cout<<"\nThe quarterly tax bill will be : "<<kist;

    // srand ( time(0));
    // int num1=rand();
    // int num2=rand();

    // int sum=num1+num2;
    // cout<<" "<<num1<<endl;
    // cout<<"+"<<num2<<endl;

    // cout<<"Press any key to get answer."<<endl;
    // _getch();

    // cout<<" "<<num1<<endl;
    // cout<<"+"<<num2<<endl;
    // cout<<"------"<<endl;
    // cout<<" "<<sum;

    // double principal,rate;
    // int t;
    // cout<<"PRINCIPAL : ";
    // cin>>principal;
    // cout<<"RATE : ";
    // cin>>rate;
    // rate/=100;
    // cout<<"Time Compounded : ";
    // cin>>t;

    // double amount,l;
    // l=pow((1+(rate/t)),t);
    // amount=principal*l;

    // cout<<endl<<"Interest Rate : "<<rate*100<<endl;
    // cout<<"Time Compounded : "<<t<<endl;
    // cout<<"PRINCIPAL : "<<principal<<endl;
    // cout<<"Interest Money : "<<amount-principal<<endl;
    // cout<<"Amount in Savings : "<<amount<<endl;

    // double loan, rate, n;
    // cout << "Loan Amount : ";
    // cin >> loan;
    // cout << "Monthly Interest Rate  : ";
    // cin >> rate;
    // rate /= 100;
    // cout << "Number Of Payments  : ";
    // cin >> n;

    // double monthly_payments;
    // monthly_payments = ((((rate)*pow((1 + rate), n))) / (pow(1 + rate, n) - 1)) * loan;

    // cout << "Monthly Payment : " << monthly_payments << endl;

    // cout << "Amount Paid Back : " << monthly_payments * 36 << endl;
    // cout << "Interest Paid : " << (monthly_payments * 36) - loan << endl;

    // double diameter;
    // const double pi=3.14159;
    // int person;
    // cout<<"Number Of People Attending The Party : ";
    // cin>>person;
    // cout<<"Diameter Of Pizza In Inches : ";
    // cin>>diameter;
    // double radius,area;
    // radius=diameter/2;
    // area=pi*pow(radius,2);
    // cout<<"Area : "<<area<<endl;
    // double slices=area/14.125;
    // cout<<setprecision(0)<<fixed;
    // cout<<"This Pizza Will Have "<<slices<<" Slices"<<endl;

    // int total_slices_needed=person*4;
    // cout<<"Total Slices Needed : "<<total_slices_needed<<endl;
    // cout<<"You Needed "<<total_slices_needed/slices<<" pizzas."<<endl;

    // double angle;
    // cout << "Enter Angle In Radians : ";
    // cin >> angle;
    // cout << setprecision(4) << fixed << showpoint;
    // cout << "Sine : " << sin(angle) << endl;
    // cout << "Cosine : " << cos(angle) << endl;
    // cout << "Tangent : " << tan(angle) << endl;

    // int share_purchased, share_sold;
    // double purchsed_price, sold_price;
    // cout << "Enter number of shares purchased : ";
    // cin >> share_purchased;
    // cout << "Enter price of each share : $";
    // cin >> purchsed_price;
    // double total_purchased_price = share_purchased * purchsed_price;
    // cout << setprecision(2) << fixed << showpoint;
    // cout << "Total Price Of Shares : $" << total_purchased_price << endl;
    // double broker_percentage, sold_share_percentage;
    // cout << "Enter Broker Percentage : ";
    // cin >> broker_percentage;
    // double broker_commission = total_purchased_price * (broker_percentage / 100);

    // cout << "Broker Commission of Purchased Shares : $" << broker_commission << endl;

    // cout << "Enter number of shares sold : ";
    // cin >> share_sold;
    // cout << "Enter price of each share sold : $";
    // cin >> sold_price;
    // double total_sold_price = share_sold * sold_price;
    // cout << setprecision(2) << fixed << showpoint;
    // cout << "Total Price Of Shares Sold : $" << total_sold_price << endl;

    // cout << "Enter Broker Percentage of sold shares : ";
    // cin >> sold_share_percentage;
    // double broker_commission_sold_shares = total_sold_price * (broker_percentage / 100);
    // cout << "Broker Commission Of Sold Shares : $" << broker_commission_sold_shares << endl;

    // double broker_total_commission = broker_commission_sold_shares + broker_commission;
    // cout << "Broker Total Commision : $" << broker_total_commission << endl;

    // double profit = (total_sold_price - total_purchased_price) - broker_total_commission;
    // cout << "Profit : $" << profit;

    string NAME, AGE, CITY, COLLEGE, PROFESSION, ANIMAL, PETNAME;
    cout << "NAME : " << endl;
    cin >> NAME;

    cout << "AGE : " << endl;
    cin >> AGE;

    cout << "CITY : " << endl;
    cin >> CITY;

    cin.ignore();

    cout << "COLLEGE : " << endl;
    getline(cin, COLLEGE);

    cout << "PROFESSION : " << endl;
    getline(cin, PROFESSION);

    cout << "ANIMAL : " << endl;
    cin >> ANIMAL;

    cout << "PETNAME : " << endl;
    cin >> PETNAME;

    cout << "There once was a person named " << NAME << " who lived in ";
    cout << CITY << "." << endl;
    cout << "At the age of ";
    cout << AGE << ", " << NAME << " went to college at " << COLLEGE << ". " << endl;
    cout << NAME << " graduated and went to work as a ";
    cout << PROFESSION << "." << endl
         << "Then, " << NAME << " adopted a(n) " << ANIMAL << " named ";
    cout << PETNAME << "." << endl
         << "They both lived happily ever after!" << endl;

    return 0;
}